# Contest video — production record

## Current production cut (2026-08-09)

The **single authoritative submission cut** is the repository-native hybrid
render. It is evidence-locked and includes receipt-bound project-function
footage from a real local `make demo` execution:

```text
demo/contest-video/story.json
  -> tools/render_contest_video.py
       -> load and cross-check committed evidence
       -> capture `make demo` from isolated `git archive HEAD`
       -> strict environment, timeout, process group, and size bounds
       -> validate transcript/events/GIF/receipt/SHA256SUMS
       -> render 1920x1080 cards with Pillow
       -> replace 20 seconds of Beat 4's static card with captured CLI playback
       -> optional macOS `say` narration (or silent AAC)
       -> FFmpeg H.264/AAC segments
       -> concat + fast-start
       -> ffprobe duration/codec validation
       -> SRT + SHA-256 + validation receipt + QA contact sheet
       -> regenerate this document's authoritative receipt block
```

Fresh production render:

```bash
python3 -m pip install -r demo/contest-video/requirements.txt
python3 tools/render_contest_video.py --self-test
python3 tools/render_contest_video.py --dry-run
python3 tools/render_contest_video.py --capture-demo
```

The fresh capture is mandatory for the authoritative production pass. A later
rebuild may reuse it only while every source-state, policy, path, executable,
and hash check still passes:

```bash
python3 tools/render_contest_video.py
```

Authoritative local artifacts:

```text
demo/out/polygraph-contest-final.mp4
demo/out/polygraph-contest-final.srt
demo/out/polygraph-contest-final.sha256
demo/out/polygraph-contest-final.validation.json
demo/out/qa/contact-sheet.jpg
```

The fixed scene timeline is 169 seconds (2:49), leaving 11 seconds of margin
inside the contest's strict three-minute limit. The renderer still validates
the encoded file rather than trusting the planned duration.

<!-- BEGIN AUTHORITATIVE VIDEO RECEIPT -->
### Authoritative rendered submission cut

<!-- Generated by tools/render_contest_video.py from the final validation sidecar. Do not hand-edit values in this block. -->

Final sidecar: `demo/out/polygraph-contest-final.validation.json`

| Property | Final post-hardening value |
|---|---|
| Repository path | `demo/out/polygraph-contest-final.mp4` |
| Encoded duration | 169.021333 s (2:49.021) |
| Margin under 180 s | 10.978667 s |
| Frame | 1920×1080 at 30/1 fps |
| Video | H.264 High, `yuv420p` |
| Audio | AAC-LC, 48000 Hz, 2 channels |
| Streaming | fast-start: `true` |
| Size | 9,239,731 bytes |
| SHA-256 | `0deb7f67697b45ac8b9b38b518d87240ac556db8bb82107f23158862372f8f30` |

| Real project-function capture | Bound value |
|---|---|
| Command | `make demo` |
| Reviewed source | `feat/contest-production-hardening-20260809` at `3bc7da203db8cec1a10df243dd75f80d53bd55f5`; `git archive HEAD`, working-tree edits excluded |
| Actual run | 7.711195 s wall time; overall exit 0; 23 PTY events |
| In final cut | 9.000 s evidence card + 20.000 s literal-output playback |
| Playback timing | Literal PTY output from the captured run; ephemeral capture-root paths and line endings are normalized, line-reveal timing is normalized for legibility, and the final frame is held. Semantic output and ordering are unchanged. |
| Capture receipt | `demo/contest-video/live-run/receipt.json`; SHA-256 `3ee881bb229b20ac3fa18c7023b5c9c512eae466835614ad54dd1bd14c2b766e` |
| Normalized ANSI transcript SHA-256 | `dd0a7c76504327c1cee1061577930867c5b8fbcfacc8b3a5bfdeb3d81c867c45` |
| Event stream SHA-256 | `6ccfc42a415b1cbc726addc3853863a3afe47d50687ec5e99112891f21cf65e3` |
| Playback GIF SHA-256 | `ed7ed35edc1e69daf07a94ac75fad54de4eb52369d745ded6ca2131d1146d818` |
<!-- END AUTHORITATIVE VIDEO RECEIPT -->

Midpoint frames from all six beats and a contact sheet are regenerated after
encoding for visual checks of wrapping, clipping, source-footer visibility,
story order, real terminal playback, the honest `MATCH` result, and the non-Arm
Pro 6000 label.

### Current story and evidence

| Beat | Claim shown | Repository source |
|---|---|---|
| baseline/banner | documented build prints `KLEIDIAI = 1`; Spark receipt has 0 compiled `kai_run_matmul` symbols | `results/server/spark-provenance.txt` |
| L3 execution | separate CUDA-host-buffer reproduction: L1/L2 identical, L3 0 → 7,968 hits with `--no-host`, 5/5 reps | `results/upstream/llamacpp-26334-cuda-host-buffer.json` |
| measured cost | 7B prefill 48.64 → 222.14 tok/s (4.57x); decode 11.17 → 18.45 (1.65x) | `results/scale/scale-experiment.json` |
| fixes/tooling | build flags restore 0 → 10 symbols; runtime workaround is separate; `polygraph check` exits 0/1/2; then real liar/honest `make demo` output | Spark provenance, Finding 4, `tools/polygraph`, live-capture receipt |
| Arm64 readiness | run `31294460364`: 1,543 requests, 0 failures, 10-minute c4 soak, 2 restarts, all 10 configured checks PASS | Arm64 summary, validation, and workflow receipts |
| honest boundary | `actualProductionTraffic:false`, `productionReady:false`, `candidateOnly:true` | Arm64 summary |

The RTX PRO 6000 Qwen3 30B-A3B campaign is mentioned only on the boundary
card. Its artifact says `armContestEvidence:false`; it is larger-model,
production-shaped **non-Arm systems evidence**, not validation of Arm or
KleidiAI dispatch.

### Reproducibility and claim safety

- Displayed figures are formatted from source JSON/text at render time.
- Beat 4 is 9 seconds of the evidence card plus 20 seconds of literal output
  derived from an actual `make demo` PTY run.
- Capture playback normalizes line-reveal pauses and holds the final frame for
  legibility; transcript text is unchanged.
- The capture bundle retains raw ANSI bytes, cleaned text, timestamped/base64
  PTY events, GIF, receipt, and validated `SHA256SUMS`.
- Capture runs with an exact environment allowlist, isolated `HOME`/`TMPDIR`,
  timeout, owned process-group termination, and transcript/event/GIF limits.
- Receipt validation rejects absolute paths, traversal, symlinks, unknown keys
  or filenames, stale branch/HEAD/archive/driver state, changed executable
  paths, altered exit contracts, and hash mismatches.
- The renderer asserts the known run ID and claim-boundary booleans before it
  creates any frame.
- It refuses stale Spark symbol counts or changed L3 repetition results.
- The story order is validated explicitly.
- Source SHA-256 hashes are recorded beside the final media receipt.
- `--tts none` removes the only host-specific step while preserving the fixed
  timeline, silent AAC track, and authored SRT.
- No generated presenter footage, stock footage, music, staged terminal
  output, or claimed CI state is introduced by this cut.

## Historical presenter/export cuts — not current

Any prior 90-second or 103-second presenter/export cut is historical only and
must not be submitted as the current contest video. The following audit is
retained solely as provenance for the preserved 90-second Remotion pipeline.

## Audit of the preserved 90-second Remotion pipeline

The prior assets are preserved outside this repository at:

```text
~/Documents/GitHub/polygraph-video-assets/
```

The audited prior final is:

```text
remotion/out/recut-2026-08-08.mp4
```

Live `ffprobe` audit on 2026-08-09:

| Property | Observed |
|---|---|
| Duration | 90.048 s |
| Frame | 1920×1080 |
| Frame rate | 30 fps |
| Video | H.264 High |
| Pixel format | `yuvj420p` |
| Audio | AAC-LC, 48 kHz, stereo |
| Size | 55,173,207 bytes |

### What the old pipeline did well

- Six deterministic 15-second scenes: 2,700 frames = 90 seconds.
- `story.json`, `cues.json`, and `layout.json` separated claims, captions, and
  measured presenter clearance from composition code.
- The 2026-08-08 recut removed the stale race and reused only two unchanged
  presenter clips.
- Presenter audio, burned-in captions, H.264/AAC encoding, and source mapping
  were already production-shaped.
- The old MP4 itself is web-playable and under the contest cap.
- The historical layout solver measured scene 02's required correction at
  `1.21`× zoom; that registered layout value remains preserved here even
  though the current evidence-card cut has no presenter-occlusion problem.

### Why it is no longer the final contest story

1. The cost card appears before the L3 executed-kernel explanation, so the
   evidence chain is out of the required order.
2. It contains no automated Arm64 readiness beat and cannot cite GitHub Actions
   run `31294460364`.
3. Its close predates the current explicit `candidateOnly` /
   `productionReady:false` boundary.
4. It does not constrain the Pro 6000 30B campaign to non-Arm systems evidence.
5. Reproduction depends on an external generated-presenter workspace,
   downloaded clips, layout state, and preserved `node_modules`, none of which
   are in this repository.
6. The package metadata still carries an old `ledgerlens` project name and its
   `npm render` script references a `src/` layout that does not match the
   preserved top-level files. The manual `npx remotion render Polygraph ...`
   command works, but the package script is not a reliable single-command
   receipt.
7. The prior full-range `yuvj420p` output is widely playable, but the current
   renderer requests standard limited-range `yuv420p` explicitly.

The prior render remains valid historical provenance. It is not deleted,
rewritten, or presented as the current submission cut. The repository-native
hybrid MP4 documented above is the only authoritative submission cut.

## Final media validation

The renderer requires:

| Check | Required |
|---|---|
| Duration | `< 180.000 s` |
| Video codec | H.264 |
| Frame size | 1920×1080 |
| Frame rate | 30/1 |
| Pixel format | 4:2:0 (`yuv420p` expected) |
| Audio | AAC, 48 kHz, stereo |
| Streaming | `moov` atom before `mdat` |

Run validation independently:

```bash
python3 tools/render_contest_video.py \
  --validate-only demo/out/polygraph-contest-final.mp4
```

The MP4 remains a local/generated contest artifact and should not be committed
unless repository policy changes; the small JSON/SRT/source files are the
auditable production inputs.
